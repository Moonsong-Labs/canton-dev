sdk-version: 2.10.2
name: canton-examples
version: 1.0.0

# Aggregate all example packages so the IDE can operate from the repo root.
source: daml

init-script: Examples:noop
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script

build-options:
  - --include=examples/invoice-workflow/daml
